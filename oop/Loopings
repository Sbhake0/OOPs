

	boolean flag = false
	if(flag){
		int num=10
	}
		
		
		
	
	
	
	Loopings..
	
	
		without curly braces maximum one line will be inside loop
		
		Selective --either
			if..else
						if(X) -- x can be any valid java stmt-- provided that
								ultimate return type is a boolean
								logical expression,boolean type,method calling
								etc
							sysout("A")
							sysout("B")
								
						if(){
							single or multiple lines inside the loop
						}
			
			
			if...
			if...else
			if..elseif...
			if..elseif...else
			if...elseif..elseif..*
			if...elseif...else
			if...elseif...elseif...*..else
			
		
			without curly brace one line that shud not be declartion or ini
				--no use -- bt with curly brace allowed
			
			if word -- X
			else shud be the last
			if shud be the first
			
			elseif..shud be followed by first if..thn can be followed by 
			elseif..
			
			
			else - is default-- shud be last
			
			
			boolean a = false
			int num= 10
			if(num==10)
				----1	
			else
				---2
				
				
			if(num==11)
				---1
				
			-----2
			
			
			switch
						-- to replace multiple if..else-- reason-- perfm
						
						jumps to the matching stmt -- will not check one by one--
						so seq doesnt matter..
						duplicates labels not allowed
						type of userchoice shud be equal to label
						
			userchoice types
					1.4 -- byte short int char
					1.5 -- 1.4 + related wrappers + enum
					1.7 -- 1.5 + String
					
				case lables values -- can be be expressions or statements
				provided that.. shud be compile time constants
					
				int num=17	
				final int a = 2
				switch(userchoice){
					case 11+a:
							---
							---
					
					case 15+a:
							---
							---
					default:
							---
							--
				}
		
		
		
			switch -- cannot be used for different types at the same type`
			limited types supported
			
			
			either -- selective
			
		
		Iterative -- till the time condition is satisfied
				for
						
						for(;;)
							sysout()
						
						for(X;Y;Z)
							sysout('')
					x - ini
						optional -- No value
						var ini
						many with same type -- with  type for first only others comma seperated
						you can any valid java stmt
						
						when ? first
						no of times -- once
						
						for(x;y;z){
							--
						}
						
						
						
					y - condition
						optional -- true
						x rules from if conditions
						when ? 2nd time
						no of times ?	1 or no of satified+1
								[provided that no break/return/exit/error]
					
					for(int n=0;n<10;i++)
						
						
					z - inc/dec
						optional -- no value
						inc/dec
						any valid java smmt -- shud be not declartion/ini
						when ? -- NA or 4th
						no of times ? 0 or no of cond is satified
						
						
				foreach -- read only -- 1.5 -- advanced loop
					for(type of element inside collection : collection)
				
				while
						int num=10
						while(num>10){
							sysout(hi)
							num++
							}
						
						do{
							--
						}while(cond);
						
						
						
						
				do..while
		
		Transfer -- navigate execution control to some other point
			break
			continue
			return
			try
			catch
			finally
			
			
			a
	
	for(int i=0;i<10;i++)
		sysout(i)
		if(a==num)
			break
		--
		--
		
		
		a=5
		for(int i=0;i<10;i++)
			----
			---
			
			if(a==i)
			   continue
	=========================================		
			